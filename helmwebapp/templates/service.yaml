apiVersion: v1
kind: Service
metadata:
  name: {{ include "helmwebapp.fullname" . }}
  labels:
    {{- include "helmwebapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helmwebapp.selectorLabels" . | nindent 4 }}
